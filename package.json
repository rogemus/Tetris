{
  "name": "tetris",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --outfile=build/tetris.bundle.js",
    "start": "esbuild src/index.ts --bundle --serve=3030 --servedir=build --outfile=build/tetris.bundle.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.11.20",
    "typescript": "^4.2.4"
  }
}
